version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: nexvi_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: nexvi_meets

volumes:
  mongo_data:
